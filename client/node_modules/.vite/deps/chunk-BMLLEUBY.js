import {
  getValidChainRPCs
} from "./chunk-I3A4RD7G.js";
import {
  init_shim
} from "./chunk-6E35MBRA.js";

// node_modules/@thirdweb-dev/wallets/dist/url-a45219bd.browser.esm.js
init_shim();
function isTwUrl(url) {
  const host = new URL(url).hostname;
  return host.endsWith(".thirdweb.com") || host.endsWith(".thirdweb-dev.com") || host === "localhost" || host === "0.0.0.0";
}
function getValidPublicRPCUrl(chain) {
  return getValidChainRPCs(chain).map((rpc) => {
    try {
      const url = new URL(rpc);
      if (url.hostname.endsWith(".thirdweb.com")) {
        url.pathname = "";
        url.search = "";
      }
      return url.toString();
    } catch (e) {
      return rpc;
    }
  });
}

export {
  isTwUrl,
  getValidPublicRPCUrl
};
//# sourceMappingURL=chunk-BMLLEUBY.js.map
